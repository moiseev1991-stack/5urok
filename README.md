# Проект "Переработка батареек"

Django приложение для учёта сданных на переработку батареек.

## Возможности

- 🔐 Регистрация и авторизация пользователей
- 📦 Ввод информации о количестве сданных батареек
- 📊 Отображение общей статистики на главной странице
- 👤 Личный профиль с историей всех сдач

## Установка и запуск

1. Активируйте виртуальное окружение:
```bash
.\venv\Scripts\activate
```

2. Установите зависимости (если ещё не установлены):
```bash
pip install -r requirements.txt
```

3. Выполните миграции:
```bash
python manage.py makemigrations
python manage.py migrate
```

4. Создайте суперпользователя (опционально, для доступа к админ-панели):
```bash
python manage.py createsuperuser
```

5. Запустите сервер разработки:
```bash
python manage.py runserver
```

6. Откройте браузер и перейдите по адресу: http://127.0.0.1:8000/

## Структура проекта

- `batteries/` - основное приложение
  - `models.py` - модель BatterySubmission для хранения данных о сданных батарейках
  - `views.py` - представления (views) для всех страниц
  - `forms.py` - формы для регистрации и ввода данных
  - `templates/` - HTML шаблоны
- `battery_recycling/` - настройки проекта Django

## Использование

1. Зарегистрируйтесь или войдите в систему
2. На странице "Сдать батарейки" введите количество сданных батареек и дату
3. На главной странице можно увидеть общее количество сданных батареек всеми пользователями
4. В профиле можно посмотреть историю всех ваших сдач
